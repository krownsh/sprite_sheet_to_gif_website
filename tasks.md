# 待辦事項 (Tasks)

- [ ] 替換 Google AdSense ID <!-- id: adsense-id -->
    - [ ] 在 `index.html` 中搜尋 `ca-pub-XXXXXXXXXXXXXXXX` 並替換為您的真實發佈商 ID (共 3 處)
    - [ ] 在 `index.html` 中搜尋 `data-ad-slot="1122334455"` 並替換為新的廣告單元 ID (右側邊欄廣告)
- [ ] 替換網域網址 (Domain) <!-- id: domain-replace -->
    - [ ] 在 `index.html` 中將 `spritesheettogif.krownsh.work` 替換為真實網址 (Canonical, Open Graph, Twitter)
    - [ ] 在 `sitemap.xml` 中將 `spritesheettogif.krownsh.work` 替換為真實網址
- [ ] 網站上架與提交 <!-- id: deploy -->
    - [ ] 將 `index.html`, `sitemap.xml`, `robots.txt` 上傳至伺服器
    - [ ] 前往 Google Search Console 提交 sitemap.xml
