# 問題與解釋紀錄 (Questions & Explanations)

## 2026-01-02 完善網站架構問題

### Q: 如何在保持一頁式 (SPA) 的情況下增加大量 Google Ads 所需的頁面？
**解釋**: 
為了符合 Google Ads 對「多個內容頁面」的要求同時維持「一頁式」架構，我將採用以下方式：
1. **動態內容切換**：在 `index.html` 中預留一個 `info-container`，預設為隱藏。當使用者點擊頁尾的「隱私權政策」或「關於我們」時，會透過 JavaScript 隱藏主工具面板並顯示該容器的內容，這在視覺上感覺像切換頁面，但實質上仍在同一個 HTML 檔案中運作，不需要重新載入。
2. **錨點連結 (Anchors)**：利用 URL hash (例如 `#privacy`) 來實現導航，這樣 Google 爬蟲可以識別不同的內容區塊。
3. **區塊化內容**：將 FAQ、關於我們、服務條款等內容直接寫入 `index.html` 的隱藏區域，或透過 `Lang` 系統動態注入，確保 SEO 抓取得到。

### Q: 是否會更動到既有的工具邏輯？
**解釋**: 
絕對不會。所有的變動都集中在 HTML 結構的增減以及連結點擊的事件監聽。既有的 `app.js` 邏輯（如畫布繪製、GIF 生成、素材工廠等）將會被完整保留，並與新的內容切換機制解耦。
### Q: 預留的 Google Ads (AdSense) 廣告位置是否還在？
**解釋**: 
是的，經過檢查，所有預留的 AdSense 廣告位置都已完整保留。我剛才更新 HTML 結構時，特別注意避開了這些區塊。具體位置如下：
1. **左側素材庫下方** (Sidebar/Library Ad): 位於 `index.html` 第 134-140 行。
2. **右側控制面板下方** (Right Sidebar/Controls Ad): 位於 `index.html` 第 220-227 行。
3. **首頁教學指南下方** (Footer/Tutorial Ad): 位於 `index.html` 第 265-272 行。
此外，`<head>` 中的 AdSense 腳本也依然存在。
